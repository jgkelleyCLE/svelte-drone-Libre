"use strict";
var devAction = require("./devAction2.js"), servers = require("./servers.js");
async function startAppDevServer(args, context) {
  const flags = args.extOptions, {
    output,
    workDir,
    cliConfig
  } = context, config = devAction.getDevServerConfig({
    flags: {
      ...flags,
      port: flags.port || "3333"
    },
    workDir,
    cliConfig,
    output
  });
  try {
    await devAction.startDevServer(config);
  } catch (err) {
    servers.gracefulServerDeath("dev", config.httpHost, config.httpPort, err);
  }
}
exports.default = startAppDevServer;
//# sourceMappingURL=devAction.js.map
